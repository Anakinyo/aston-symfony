{% extends 'AstonBackBundle::layout.html.twig' %}

{% block body %}
  <h1>{{ 'category detail'|trans }}</h1>

  <table class="table">   
    <tr>
      <td colspan="8">
        <a href="{{ path('aston_back_category_update', {id: category.id}) }}" class="btn btn-xs btn-default">Editer</a>
        <a href="{{ path('aston_back_category_delete', {id: category.id}) }}" class="btn btn-xs btn-danger">Supprimer</a>
      </td>
    </tr>
    <tr>
      <td>{{ 'Name'|trans }}</td>
      <td>{{ category.name }}</td>
      <td></td>      
      <td>{{ 'ID'|trans }}</td>
      <td>{{ category.id }}</td>
    </tr>    
    <tr>
      <td colspan="8">
        <a href="{{ path('aston_back_category_update', {id: category.id}) }}" class="btn btn-xs btn-default">Editer</a>
        <a href="{{ path('aston_back_category_delete', {id: category.id}) }}" class="btn btn-xs btn-danger">Supprimer</a>
      </td>
    </tr>
  </table>
{% endblock body %}